# test GLS partition

expect_true(FALSE, info = paste("need to write appropriate tests",
                                "for fitGLS.partition_cpp()"))
expect_true(FALSE, info = "need to depricate fitGLS.partition()")
expect_true(FALSE, info = paste("need to create appropriate tests for",
                                "correlated.F.bootstrap()"))
expect_true(FALSE, info = paste("need to create appropriate tests for",
                                "GLS.partition.pvalue"))
