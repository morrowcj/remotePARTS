# Tests for fitGLS

# coords = cbind(x = rnorm(10), y = rnorm(10))
# D <- as.matrix(geosphere::distm(coords))
# X <- matrix(rnorm(50), ncol = 5)
# V <- cor(X)

expect_true(FALSE, info = paste("Need to write working tests for fitGLS.R",
                                "after writing the appropriate tests for",
                                "fit_spatialcor() and fitV()")
)

