# test SpatCor functions

expect_true(FALSE, info = "need to come up with tests for taper_sphere()")
expect_true(FALSE, info = "need to come up with tests for fit_spatialcor()")
expect_true(FALSE, info = "need to create methods and classes for fit_spatialcor()")

# test fitV / fit.V
expect_true(FALSE, info = paste("need to write appropriate tests for each of ",
                                "fitV's transformation functions"))
expect_true(FALSE, info = "need to depricate V.fit")
