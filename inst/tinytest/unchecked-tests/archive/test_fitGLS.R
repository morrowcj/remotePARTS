# Tests for fitGLS

# load("data-for-tests.rda")

## missing functionality
expect_true(FALSE, info = paste("Need to write working tests for fitGLS.R",
                                "after writing the appropriate tests for",
                                "fit_spatialcor() and fitV()")
)
expect_true(FALSE, info = paste("need to add missing statistics to fitGLS()",
                                "output."))
expect_true(FALSE, info = "need to create starmod.gls class and methods")

## R version
expect_true(FALSE, info = "need to depricate fitGLS_R()")
